// Este fichero existió como store separado de tema, pero la preferencia se
// guarda ahora dentro de `useRemindersStore` (ver remindersStore.ts).
// Lo dejamos como módulo vacío para que cualquier import antiguo no rompa
// el build.
export {};
